#include <stdio.h>
#include <math.h>
#include <cs50.h>

float quaters, dimes, nickles, pennies;
int get_positive_change(string prompt);
float change;


int main(void)
{
     float n;
    do
        {
        n = get_float("cuanto se debe dar?");
        }
        while (n < 0);
        printf("%f", n);
}
   
   
